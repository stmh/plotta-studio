[package]
name = "sketch-003-text"
version.workspace = true
edition.workspace = true

[features]
default = []
# Enable AxiDraw plotting (requires libudev on Linux)
hardware = ["drawing-plotter/hardware"]

[dependencies]
sketch-runner = { workspace = true }
drawing-svg = { workspace = true }
drawing-text = { workspace = true }
drawing-plotter = { workspace = true, default-features = false }
